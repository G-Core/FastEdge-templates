name: Publish harden-cookies

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'harden-cookies/**'

permissions:
  contents: read

jobs:
  publish:
    uses: ./.github/workflows/_publish_rs.yml
    with:
      template: harden-cookies
    secrets: inherit
