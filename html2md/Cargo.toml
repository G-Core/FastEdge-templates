[package]
name = "html2md"
version = "0.1.0"
edition = "2021"

[dependencies]
proxy-wasm = { workspace = true }
htmd = "0.5"
htmlize = { version = "1", features = ["unescape"] }

[lib]
crate-type = ["cdylib"]
